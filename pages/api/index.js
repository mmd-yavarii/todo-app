export default function handler(req, res) {
  switch (req.method) {
    case 'GET':
      // code
      break;

    case 'POST':
      // code
      break;

    case 'DELETE':
      // code
      break;

    case 'PATCH':
      //code
      break;

    case 'PUT':
      // code
      break;
  }
}
